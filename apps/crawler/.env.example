# Crawl4AI Microservice Configuration
CRAWLER_HOST=127.0.0.1
CRAWLER_PORT=3001

# Optional: Proxy settings (if needed for WAF bypass)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
